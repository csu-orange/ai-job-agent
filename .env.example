PORT=3000

# Optional. When omitted, the app uses the built-in local analysis engine.
OPENAI_API_KEY=

# Optional. Keep model selection configurable so the app is not tied to one model.
OPENAI_MODEL=gpt-4.1-mini
